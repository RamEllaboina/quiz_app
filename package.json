{
    "name": "quiz-app-deployment",
    "version": "1.0.0",
    "description": "Root deployment script",
    "scripts": {
        "build": "echo 'No build step needed for static frontend'",
        "postinstall": "cd backend && npm install",
        "start": "cd backend && node server.js"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "express": "^4.18.2",
        "mongoose": "^7.5.0",
        "cors": "^2.8.5",
        "serverless-http": "^3.0.1",
        "dotenv": "^16.3.1",
        "bcryptjs": "^2.4.3",
        "jsonwebtoken": "^9.0.2"
    }
}